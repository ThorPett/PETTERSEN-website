---
interface Props {
  /** Sobre fondo navy. */
  onDark?: boolean;
}
const { onDark = false } = Astro.props;
---

<div class:list={['kicker', onDark && 'kicker--dark']}>
  <span class="kicker__rule" aria-hidden="true"></span>
  <span class="kicker__txt"><slot /></span>
</div>
